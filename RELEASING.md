# Releasing use-sql

`use-sql` is experimental while it remains below `0.3.0`.

This repository is scaffolded for future RustUse release-readiness checks, but the initial implementation is not published by this task. When publishing is explicitly requested later, publish focused crates first and the `use-sql` facade last after registry propagation.
